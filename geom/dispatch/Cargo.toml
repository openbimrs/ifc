[package]
name = "geom-dispatch"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Runtime backend selection. The only crate that knows every backend exists."

[dependencies]
geom-core.workspace = true
geom-kernel.workspace = true
geom-cpu.workspace = true
geom-simd.workspace = true
geom-gpu.workspace = true

[features]
default = []
# Propagates to geom-gpu; nothing else changes.
gpu = ["geom-gpu/gpu"]
