[package]
name = "geom-kernel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "The geometry kernel CONTRACT: traits only. The swap boundary."

# Depends ONLY on the data crate. No backend may appear here — that is what
# makes backends (and this whole kernel) replaceable.
[dependencies]
geom-core.workspace = true
thiserror.workspace = true
