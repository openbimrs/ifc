# nehirde

Rust workspace scaffold. See `HERMES.md` at the repo root for the durable
project brief (what this is, layout, conventions) and `docs/ROADMAP.md` for
where the work stands. Agent-facing context is layered progressively — read
`HERMES.md` first, then the nested `AGENTS.md` in the subtree you're touching.
