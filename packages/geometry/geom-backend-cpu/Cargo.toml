[package]
name = "geom-backend-cpu"
description = "Portable and runtime-optimized CPU execution context for Nehirde geometry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[features]
default = []
simd = []
parallel = ["dep:rayon"]

[dependencies]
rayon = { workspace = true, optional = true }
