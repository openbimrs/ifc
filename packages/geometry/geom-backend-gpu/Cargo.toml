[package]
name = "geom-backend-gpu"
description = "GPU executor adapter contract for batched Nehirde geometry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[features]
default = []

[dependencies]
geom-mesh.workspace = true
geom-model.workspace = true
geom-kernel = { workspace = true, features = ["model"] }

[dev-dependencies]
geom-core.workspace = true
# The out-of-tree executor gate builds a simulated native backend using only
# published items, proving a real CUDA/HIP bridge needs no privileged access.
geom-model.workspace = true
geom-mesh.workspace = true
