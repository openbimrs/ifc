[package]
name = "geom-kernel"
description = "Backend-neutral geometry operation contracts"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[features]
default = []
mesh-boolean = ["dep:geom-mesh"]
model = ["dep:geom-mesh", "dep:geom-model"]

[dependencies]
geom-core.workspace = true
geom-mesh = { workspace = true, optional = true }
geom-model = { workspace = true, optional = true }
thiserror.workspace = true
